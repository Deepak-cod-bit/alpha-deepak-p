#include <stdio.h>
int main(){
    int age = 45;
    if(age>10){
        printf("we are inside is");
        printf("your age is greater than 21");
    }
    if(age%5 ==0){
        printf("we are inside");
        printf("your age is greater than 10");
    }
}