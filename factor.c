#include<stdio.h>
void main(){
    int num,i,j;
    printf("enter the nmber to find fact:");
    scanf("%d",&num);
    printf("factors of the number %d are:",num);
    if(num%  i == 0){
        printf("%d",i);
    }
}